Goal 3: Deploying Argo Rollouts via GitOps
- Install Argo Rollouts to central-hub cluster via ArgoCD App-of-Apps pattern.
- Create a new namespace for Argo Rollouts: `argorollouts`.
- Deploy Argo Rollouts to the `argorollouts` namespace.
- Verify the installation by checking the Argo Rollouts UI.

Goal 3.5: Deploy Argo Events to central-hub cluster via ArgoCD App-of-Apps pattern.
- Create a new namespace for Argo Events: `argoevents`.
- Deploy Argo Events to the `argoevents` namespace.
- Verify the installation by checking the Argo Events UI.
